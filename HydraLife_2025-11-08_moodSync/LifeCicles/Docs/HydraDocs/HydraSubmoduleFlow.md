## Submódulo removido: Colorful-Plasma-Themes

O repositório de temas foi inicialmente integrado como submódulo, mas após conflitos e limitações, foi decidido tratá-lo como clone direto com remotos `origin` e `upstream`.

### Ação

- Submódulo removido
- Script `update-themes.sh` atualizado para usar `git fetch upstream` + `git push origin`

---


